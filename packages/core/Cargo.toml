[package]
name = "dabar-core"
version = "0.1.0"
edition.workspace = true
license.workspace = true

[dependencies]
anyhow.workspace = true
chrono.workspace = true
reqwest.workspace = true
serde.workspace = true
serde_json.workspace = true
tokio.workspace = true
tracing = "0.1"
uuid.workspace = true
# whisper-rs: Rust bindings to whisper.cpp for offline GGML transcription.
# Builds whisper.cpp from bundled sources — no system install required.
whisper-rs = "0.14"

